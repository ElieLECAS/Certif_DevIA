version: "3.8"

services:
    # Base de données PostgreSQL
    postgres:
        image: postgres:15
        container_name: gmao-postgres-prod
        environment:
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            TZ: Europe/Paris
            # Variables pour le superuser (optionnelles)
            SUPERUSER_USERNAME: ${SUPERUSER_USERNAME:-}
            SUPERUSER_EMAIL: ${SUPERUSER_EMAIL:-}
            SUPERUSER_NOM_COMPLET: ${SUPERUSER_NOM_COMPLET:-}
            SUPERUSER_PASSWORD: ${SUPERUSER_PASSWORD:-}
            SUPERUSER_ROLE: ${SUPERUSER_ROLE:-}
        restart: unless-stopped
        healthcheck:
            test:
                [
                    "CMD-SHELL",
                    "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}",
                ]
            interval: 30s
            timeout: 10s
            retries: 6

    # API FastAPI avec interface web intégrée - IMAGE DOCKER HUB
    api:
        image: elielecas/gmao-api:correct
        container_name: gmao-api-web-prod
        ports:
            - "8081:8000" # Interface web sur le port 8080
        environment:
            - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
            - PYTHONUNBUFFERED=1
            - SUPERUSER_USERNAME=${SUPERUSER_USERNAME:-}
            - SUPERUSER_EMAIL=${SUPERUSER_EMAIL:-}
            - SUPERUSER_NOM_COMPLET=${SUPERUSER_NOM_COMPLET:-}
            - SUPERUSER_PASSWORD=${SUPERUSER_PASSWORD:-}
            - SUPERUSER_ROLE=${SUPERUSER_ROLE:-}
        depends_on:
            postgres:
                condition: service_healthy
        restart: unless-stopped

